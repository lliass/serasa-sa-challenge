import { ControllerConfiguration } from '../../common/interfaces/controller.interface';

const producerControllerConfig: ControllerConfiguration = {
  baseEndPoint: 'farm',
};

export default producerControllerConfig;
